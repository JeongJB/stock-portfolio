# 프로덕션 배포 시 백엔드 API 절대 URL 지정 (예: https://api.example.com).
# 비워두면 같은 origin 의 /api 경로로 요청 → dev 에서는 vite proxy 가 8080 으로 포워딩.
VITE_API_BASE_URL=

# API Gateway 단일 키 (1인용). 비워두면 헤더 미부착.
# VITE_API_KEY=
